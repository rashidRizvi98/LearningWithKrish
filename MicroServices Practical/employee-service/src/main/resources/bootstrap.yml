spring:
  cloud:
    config:
      uri: http://localhost:8181
  application:
    name: employee-service

eureka:
  instance:
    instance-id: ${spring.application.name}-${random.int}
    hostname: localhost
server:
  port: 0